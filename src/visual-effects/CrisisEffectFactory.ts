import { Vector3 } from '../types/core';

export class CrisisEffectFactory {
  createCrisisEffect(type: string, severity: number, center: Vector3): any {
    // Stub implementation
    return {};
  }
}