import { Vector3 } from '../types/core';

export class ConnectionEffectFactory {
  createConnectionEffect(from: Vector3, to: Vector3, type: string): any {
    // Stub implementation
    return {};
  }
}